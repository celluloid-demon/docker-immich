services:
  immich-server:
    env_file:
      - ${SECRETS}/database.env
      - ${SECRETS}/redis.env

  immich-machine-learning:
    env_file:
      - ${SECRETS}/database.env
      - ${SECRETS}/redis.env

  redis:
    env_file:
      - ${SECRETS}/redis.env

  database:
    env_file:
      - ${SECRETS}/database.env
